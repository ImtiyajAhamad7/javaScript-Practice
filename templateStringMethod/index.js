// es6 string methods

const name = "imtiyajAhamad";
console.log(`${name}`.startsWith("it"));   //starts with check for string start with which char or sunstring starts  returns boolean
console.log(`${name}`.endsWith("j"));    //ends with check for string end with which char or sunstring ends   returns boolean
console.log(`${name}`.includes("j"));    //includes check for string contain the requred string or character   returns boolean
console.log(`${name} `.repeat(10));   //repeat the string